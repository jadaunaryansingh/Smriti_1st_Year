{
  "buildCommand": "pnpm run build:client",
  "outputDirectory": "dist/spa",
  "devCommand": "pnpm dev",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
